<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jeeplus.modules.customer.dao.CustomerDao">
    
	<sql id="customerColumns">
		a.id AS "id",
		a.customer_no AS "customerNo",
		a.mobile AS "mobile",
		a.type AS "type",
		a.create_by AS "createBy.id",
		a.create_date AS "createDate",
		a.update_by AS "updateBy.id",
		a.update_date AS "updateDate",
		a.remarks AS "remarks",
		a.del_flag AS "delFlag",
		a.name AS "name",
		a.short_name AS "shortName",
		a.email AS "email",
		a.fax AS "fax",
		a.address AS "address",
		a.alipay AS "alipay",
		a.wechat_pay AS "wechatPay",
		a.last_order_state AS "lastOrderState",
		a.reg_time AS "regTime",
		a.state AS "state",
		a.vm_id AS "vmGroup.id"
	</sql>
	
	<sql id="customerJoins">
	</sql>
	
    
	<select id="get" resultType="Customer" >
		SELECT 
			<include refid="customerColumns"/>
		FROM t_customer a
		<include refid="customerJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="Customer" >
		SELECT 
			<include refid="customerColumns"/>
		FROM t_customer a
		<include refid="customerJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="Customer" >
		SELECT 
			<include refid="customerColumns"/>
		FROM t_customer a
		<include refid="customerJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO t_customer(
			id,
			customer_no,
			mobile,
			type,
			create_by,
			create_date,
			update_by,
			update_date,
			remarks,
			del_flag,
			name,
			short_name,
			email,
			fax,
			address,
			alipay,
			wechat_pay,
			last_order_state,
			reg_time,
			state,
			vm_id
		) VALUES (
			#{id},
			#{customerNo},
			#{mobile},
			#{type},
			#{createBy.id},
			#{createDate},
			#{updateBy.id},
			#{updateDate},
			#{remarks},
			#{delFlag},
			#{name},
			#{shortName},
			#{email},
			#{fax},
			#{address},
			#{alipay},
			#{wechatPay},
			#{lastOrderState},
			#{regTime},
			#{state},
			#{vmGroup.id}
		)
	</insert>
	
	<update id="update">
		UPDATE t_customer SET 	
			customer_no = #{customerNo},
			mobile = #{mobile},
			type = #{type},
			update_by = #{updateBy.id},
			update_date = #{updateDate},
			remarks = #{remarks},
			name = #{name},
			short_name = #{shortName},
			email = #{email},
			fax = #{fax},
			address = #{address},
			alipay = #{alipay},
			wechat_pay = #{wechatPay},
			last_order_state = #{lastOrderState},
			reg_time = #{regTime},
			state = #{state},
			vm_id = #{vmGroup.id}
		WHERE id = #{id}
	</update>
	
	
	<!--物理删除-->
	<update id="delete">
		DELETE FROM t_customer
		WHERE id = #{id}
	</update>
	
	<!--逻辑删除-->
	<update id="deleteByLogic">
		UPDATE t_customer SET 
			del_flag = #{DEL_FLAG_DELETE}
		WHERE id = #{id}
	</update>
	
	
	<!-- 根据实体名称和字段名称和字段值获取唯一记录 -->
	<select id="findUniqueByProperty" resultType="Customer" statementType="STATEMENT">
		select * FROM t_customer  where ${propertyName} = '${value}'
	</select>
	
</mapper>